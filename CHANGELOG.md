# Changelog

## Housekeeping — 2026-05-27

- **Branch cleanup**: deleted 41 merged-via-PR branches from the remote (`gh api -X DELETE refs/heads/...`) and pruned local tracking. All `*-detail-layer`, `changelog-*`, `hotfix-*`, and earlier phase/refactor branches that had landed via squash-merge are gone; only `main` remains on `origin`. No code change — production unchanged (live SHA-256 `6f6530e6…`, main HEAD `fb69666`). Branch list was getting long enough to make `gh pr` / `git branch` output noisy.
- **Branch cleanup #2**: deleted 8 merged-via-PR branches from the remote (`agent-scorecard-detail-layer`, `agent-scorecard-hourly-fix`, `changelog-pr134-135`, `changelog-pr137-138`, `changelog-pr140`, `exclude-team-leaders`, `hourly-observation-relabel`, `qa-performance-with-sampling`) and pruned local tracking. Covers PRs #134–#141 plus their CHANGELOG follow-ups. Only `main` remains on `origin`. No code change — production unchanged (live SHA-256 `f919bac9…a54c`, main HEAD `654d18f`).

## Vivid detail layers — 2026-05-26

Hourly Output, Overview, and Daily Intake pages brought in line with the Vivid mockup.

- **PR #108 — Hourly Output detail layer** (GitHub #106, merge `f7dc82b`): 4 KPI cards reactive to the team filter, team chips replacing the dropdown, sort tabs, agent avatars + team label, PER HOUR row moved to top with peak highlight, per-team heatmap colouring, three insight cards (DAY SHAPE / AT RISK / LATE ACTIVITY), saturation legend.
- **PR #109 — DoD% delta on the Hourly Output hero card** (GitHub #108, merge `8d0ad93`): fetches yesterday's snapshot once per page mount, renders ±X.X% DoD pill on the hero KPI when "All teams" is active, hidden otherwise.
- **PR #110 — Overview detail layer** (GitHub #107, merge `d035895`): editorial page head with Export CSV + Open BO QA buttons, curated 4-card hero KPI row (backlog with 8-day trend bars + DoD%, tagged with DoD%, sampling and reject with progress bars), enriched lifecycle footer, 2-column body — agent productivity scorecard with filter tabs, teams-vs-floor mini-panel, open alerts and per-team chart on the left, daily verdict checklist with auto-snapshot countdown on the right, collapsed "Detailed metrics" expander for the other 11 KPI cards.
- **PR #111 — Overview hero card: Set to Valid today** (GitHub #110, merge `2d326ff`): repoints the gradient hero KPI from BO QA backlog to today's "set to Valid" count (`valid_today_support` + `valid_today_io` across both tables). The 8-day mini-bar trend and ±X.X% DoD pill are repointed to the same metric; the duplicate "Set to Valid today" entry in the detailed-metrics expander is removed (summary count drops from 11 to 10).
- **PR #113 — Daily Intake detail layer** (GitHub #113, merge `96a2c65`): editorial page head with Export CSV + Manual upload buttons, filter tabs (All / Today's intake / In progress / Completed / Sanctions) with counts, four hero KPI cards (intake purple-gradient hero with 8-day trend bars + DoD%, active tasks, add-new-company, cleared today), a featured Today's-batch card (status pill, tagging-% progress ring, team chips + agent avatars, TAGGED / IN BO QA / VALID stat blocks), an Other-active-batches grid filtered by the active tab, a team allocation panel with per-team stacked bars against the 280×agents floor, a 4-day upload timeline from snapshots, and the existing monthly-intake + Valid-by-table charts moved into a Charts & trends expander (recoloured to the Vivid palette).
- **PR #114 — Tasks page chrome (editorial head + hero KPIs)** (GitHub #115, merge `8322a38`): editorial page head + 4-card hero KPI row (Active tasks, Records in flight, Properly completed %, Median TAT) with Export CSV + Open-drawer action buttons, replacing the old "Task Lifecycle Tracking" heading and flag-KPI row; the tasks table, filters, and drawer below are left unchanged.
- **PR #115 — Tasks headline: "Task lifecycle"** (GitHub #116, merge `7a4f324`): Tasks page-title goes from `<em>Tasks</em>` to `Task <em>lifecycle</em>`, adopting the first-word-plain / second-word-serif-italic pattern used on Overview / Hourly Output / Daily Intake.
- **PR #117 — Tasks detail layer** (GitHub #118, merge `54c9487`): 4 enriched hero cards (Active tasks gradient hero + DoD chip + sublines, Records-in-flight with Waiting/Tagged/BO-QA stage segbar, Properly completed with progress bar, Median TAT with team-bottleneck), team filter chip row, restyled tabbed task list (Active / Flagged / BO QA pending / Sanctions / Completed (7d)), and a 6-panel selected-task drawer (agent allocation, status donut, QA reviewers, turnaround per task, flag breakdown, dead-vessel ratio).
- **PR #118 — Completed Tasks detail layer** (GitHub #119, merge `27637f4`): 4 hero cards with mini-bar history + sparkline + real DoD deltas from the `ownership_task_history` ledger, team filter chip row, tabbed paginated completion archive (All / Sanctions / 100% QA / TAT breach / Resubmitted) with status pills, selected-batch peak-day drawer (3 panels), Throughput & turnaround section (weekly dual-line + 6-bucket TAT distribution + SLA banner), QA reviewer leaderboard with batches-touched heatmap.
- **PR #120 — New Companies tagging analytics** (GitHub #121, merge `8b51d74`): replaces the bare new-companies table with a PM dashboard, reframed from a "decision queue" to a "tagging analytics log" (`add_new_company_open`=0, most records already Valid) at day granularity (no per-record timestamps in the data) — real STATE fields only, no fabrication. Editorial page head + Live pill + team/status chip row; 4-card hero (Total tagged, Still open `verification_status ∉ {Valid, Done}`, Cleared to Valid, Quality gaps from `case_company_gap` + `case_no_imo`); 3-panel Breakdowns (days-open distribution 0d/1d/2-3d/4-7d/8+d, recent-tags feed by `days_open` asc, queue-health mini-cards + top taggers); selected-record drawer (Record detail / Tagger stats / Team context with 5-team comparison + role distribution); paginated companies queue + tagger roster; read-only "decisions happen in Airtable" footer. Untracked-data mockup sections (per-hour intake, source/person/nationality, duplicate similarity, reviewer comments, decision categories) each swapped for a real-data substitution rather than placeholdered.
- **PR #122 — Pipeline & lead-time detail layer** (GitHub #123, merge `786a159`): replaces the pipeline page with a Vivid detail layer — per-stage transit timings aren't tracked, so every stage-P50/P90 / WoW / stage×team-P50 mockup section is substituted with real current-volume / current-age metrics (from `tasks_all` status_distribution + `date_first_seen`), no fabrication. Editorial page head + Live pill + team/status chip row; largest-backlog banner (renders only when one stage holds >2× the median stage volume); 4-card hero (End-to-end P50 from `lead_time_today`, Largest backlog stage with 5-stage segbar, Largest backlog volume, Stuck >48h); 5 stage cards (Waiting/Tagged/BO QA/WW QA/Done) with current count + median/max age + BOTTLENECK highlight; 4 summary cards (Records in pipeline / 24h throughput / Flow efficiency / Stuck > SLA); "Stage volumes & ages" (per-stage volume bars + team×stage current-count heatmap, green→red); "Stuck records · past SLA" tabbed table (dominant-stage rows, SLA-exceed delta, flow segments, visual Unblock/Reassign); "Trend & classification" (14d throughput + median-TAT chart from `STATE.completedTasks` with SLA dashed line + Flow Framework v3 A/B/C/D from `totals.flow_*_today`). WoW / per-stage-timing deltas render "—".
- **PR #124 — BO QA Console detail layer** (GitHub #125, merge `acfce76`): replaces the BO QA console with a Vivid detail layer — sampling-shortfall %, TOP/MID/BOT stratum bands, per-event reviewer feed, current→proposed field diff, source documents, comment threads, decision-keyboard workflow, and per-record sanctions flag aren't tracked, so each is substituted with a real-data equivalent, no fabrication. Editorial page head + Live pill + team/cohort chip row; backlog banner (renders when backlog>0 and 0 reviewed today); 4-card hero (In queue from `bo_qa_backlog`, Inspected today from `qa_today_all`, Coverage proxy, Reject rate); reviewer cards from `qa_reviewers` (all-time reviews + median/p90 review time); 3 cohort cards (Add-new-company / Company-gap / No-IMO from the respective case-record arrays); mandatory-100%-QA banner; tabbed worklist (Pending/Approved/Changed/Returned) over the unified case-records pool with team filter + pagination; selected-record drawer (Record detail real fields / Comment context with source array + shared count / Audit trail of tagged-by + qa-assigned + qa-status events); Today's coverage (`qa_hourly` chart with empty-state, comment-distribution donut excluding "(no comment)", sanctions-cohort fallback table). WoW / per-reviewer-P50 deltas render "—".
- **PR #128 — BO QA Console: QA-disposition worklist** (GitHub #128, merge `d38eaad`): pivots the BO QA worklist off the cohort-record pool (which duplicated the New Companies page) to a QA-queue worklist grouped by disposition — a data audit found `qa_status` is `approve`/null only (zero changes recorded, ~100% approval), so a "QA-changed records" view can't be honestly populated, no fabrication. Worklist source now unions every `aggregates_v2` `*_records` array carrying a `qa_status` field (today only `add_new_company_records`; auto-includes future arrays); tabs reframed by disposition (Pending QA / Needs update from `verification_status == "need to be update"` / Approved / All reviewed), with the empty Changed/Returned tabs removed; honest sub-header surfaces the real approval rate and notes QA-flagged changes aren't recorded as such in the source; columns reframed (IMO + copy / Role / Company + agent / QA reviewer / QA state pill / Verification chip / Age / visual actions); drawer reframed to Record detail / QA disposition (state pill + plain-language meaning + per-reviewer all-time stats from `qa_reviewers`) / Audit trail (real events only — tagged-by → qa-assigned → qa-status → verification), no before/after field diffs. Page chrome, hero KPIs, reviewer cards, cohort cards, mandatory-QA banner, and Today's Coverage panels untouched.
- **PR #131 — Tasks + Completed: slide-out side panel** (GitHub #131, merge `a918637`): repositions the Tasks and Completed Tasks detail drawer from a "renders below the list" pattern to a right-side slide-out panel — new shared `.side-panel-*` CSS (520px desktop / full-width mobile, fixed-position, internally scrolling body, Vivid `--vivid-grad` header accent, body scroll-lock via `.side-panel-open`, z-index backdrop 50 / panel 51, 220ms slide-in). Close via × in the sticky header, backdrop click, or Escape (wired once in `_wireSidePanel()` from `init()`); page navigation auto-dismisses; in-page re-renders (filter/tab changes) restore the panel without closing. Tasks (6 panels: agent allocation, status donut, QA reviewers, turnaround, flag breakdown, dead-vessel ratio) and Completed (3 panels: peak-day hour-by-hour, top agents, QA reviewer history) moved inside; `#tlDetail`/`#ctDetail` removed from the page DOM. `STATE.sidePanelOpen` synced with `selectedTaskId`/`completedSelectedId`. BO QA Console / New Companies / Pipeline drawers untouched (still below-list).
- **PR #132 — QA Hourly Output detail layer** (GitHub #132, merge `d3747a5`): replaces `PAGE_RENDERERS.qa_hourly` with a Vivid detail layer reusing the #131 slide-out (`.side-panel-*`) for the selected-reviewer detail. Editorial page head + Live pill + filter chips (All reviewers / Idle >60m / Active); idle-reviewer alert banner (renders only when ≥1 reviewer idle); 4-card hero (Reviews today from `qa_today_all.reviewed`, Peak hour from `qa_hourly`, P50 review time from median of `qa_reviewers[].median_review_time_hours`, Active QAs · now); reviewer × hour heatmap from `qa_hourly` with per-reviewer-tinted cells (shaded by volume), bordered peak-hour cell, dashed-orange idle cell, diagonal-stripe future cells, scale legend; row click → side panel with 3 stacked panels (Hour-by-hour bars from `qa_hourly`, Decisions donut from `by_qa[name]` approve/change, Reviews by source team from `by_team` + cohort split Sanctions/Standard from `by_qa[name].qa_inspected_today_sanctions/_non_sanctions`); Idle reviewers & Today's pace panels; existing companies-by-hour heatmap retained below. Untracked mockup sections substituted, no fabrication: per-team cell tint → per-reviewer color (no team mapping in aggregates_v2), per-team chips omitted, "In queue {N}" → real `by_qa[name].bo_qa_backlog`, "Streak {N} days" → "—", "Last review … IMO X" → last-active hour from `qa_hourly`, "162 redirected" → real `totals.bo_qa_backlog`, per-decision per-hour → total reviews/hour + footnote, Return % → "—" (no Return status), per-reviewer source team → global `by_team`, TOP/MID/BOT stratum → Sanctions vs Standard per reviewer, P50 minutes/0–10m bar → median hours (bar 0→P90), 8-day bars / DoD → today's per-hour / "—".
- **PR #134 — QA Performance detail layer + per-reviewer sampling % (team-based, yesterday cohort)** (GitHub #134, merge `002e005`): combined pipeline + dashboard. Aggregator (`.poll_work/aggregate_v2.py`): new `_build_qa_team_map(roster)` reads `roster["teams"][T].qa.name` for the 5 team-QAs (case-insensitive); `compute_qa_reviewers()` now emits per-reviewer `assigned_team` / `eligible_pool_today` / `reviews_today` / `sampling_pct_today` / `sampling_target_pct`, denominator = yesterday's tagged-into-BO-QA cohort (mirrors `bo_qa_coverage`'s next-day-review model — work reviewed today was tagged yesterday). Team-QAs get the 25% floor (`SAMPLING_TARGET_PCT`); the 2 cross-team Experts (Simon Francis, Irene Njuguna, from `EXPERT_NAMES`) get a sanctions-cohort denominator + 100% target; strict numerator (only the reviewer's reviews of their assigned cohort); defensive nulls when a reviewer has no team mapping and isn't an expert (no fabrication). Removed the circular `by_qa.sampling_actual_pct` (denominator = reviewer's own already-reviewed slice → ~0) from `QA_KEYS`; kept the correct `by_team.sampling_actual_pct`. Dashboard (`deploy/index.html`): replaces `PAGE_RENDERERS.qa_performance` with a Vivid detail layer reusing the PR #131 slide-out for the deep-dive — editorial page head + Live pill + filter chips (All / Below 25% sampling / Above 30% change rate) + low-approval banner (any reviewer < 90%); 4-card hero (Avg sampling % vs 25%, Avg change rate, Reviews/hour from `qa_today_all` ÷ elapsed hours, At-risk reviewers); signal-coloured reviewer scorecards (green/orange/red by sampling + change rate, with sampling/approval/change/TAT rows + `qa_hourly` mini-bars); reviewer comparison table with target marker (25% team-QAs, 100% experts); selected-reviewer deep-dive (3 stacked: Today hourly from `qa_hourly`, Decisions donut, Sampling detail with eligible pool + reviewed + target + status); Today's snapshot (sampling distribution, change-rate distribution, pool summary); defensive "(aggregator pending)" path when the cache predates the new fields. 139 unit tests pass; backward-compatible.
- **PR #135 — Exclude team leaders from QA reviewer aggregation** (GitHub #135, merge `dc6fbba`): `config/roster.json` adds a top-level `"team_leaders": [{ "name": "James Mwangi" }]` array; `.poll_work/aggregate_v2.py` adds `_load_team_leaders(here)` returning a lowercased exclude-set, threaded through `aggregate()` as a new optional `qa_exclude` param into `compute_metrics` / `compute_qa_reviewers` / `compute_qa_hourly` / `compute_qa_today_all`, plus a leader-key filter on the `by_qa` slice. Team leaders are treated as non-QA entirely — dropped from `qa_reviewers[]` / `qa_hourly{}` / `by_qa{}` and their verdicts excluded from `qa_today_all` + `totals.qa_inspected_today` / `qa_changed_today` / reject rates. Exact full-name case-insensitive match (so the unrelated Tembo tagger "James Maina" is unaffected); `by_team` / `by_agent` / flow framework / `bo_qa_coverage` / `not_yet_finalized` left untouched; backward-compatible (new params default to an empty set); 139 tests pass. No dashboard change needed — verified live post-merge: 8 `qa_reviewers` incl. James → 7 with James gone from `qa_reviewers`/`qa_hourly`/`by_qa`.
- **PR #137 — Agent Scorecard detail layer with per-agent QA approval rate** (GitHub #137, merge `7ec1366`): replaces `PAGE_RENDERERS.agent` with a Vivid detail layer reusing the PR #131 slide-out for the team-rank deep-dive. Editorial page head + Live pill + Compare/Export/Send-message (Compare & Send visual); selected-agent card (avatar + team colour, "#{rank} in team" chip → opens deep-dive, active/idle pill [active if `tagged_today`>0], meta = tagged-today + manager from `roster.teams[team].qa.name` + team, Prev/Next nav + Switch dropdown over `by_agent` sorted by `tagged_today`); 4 hero KPIs — Tagged Today (real `by_agent[name].tagged_today` + cohort-avg chip + team-colour mini-bars), Today vs Floor (substitute for 30-day chart: real `tagged_today` vs floor 280 + above/below signal + progress bar + cohort-avg sub-line), QA Approval Today (REAL per-agent from `qa_inspected_today`/`qa_changed_today`; renders "—" + "QA hasn't reviewed today's tags yet" when `qa_inspected_today`=0, else approval % with signal colour + approved/changed sub-line + cohort-avg footer), Records/Hour (substitute for P50 tag time: `tagged_today` ÷ EAT hours elapsed + cohort comparison); QA Decisions Today donut (REAL per agent — approve vs changed slices, "{N} reviewed" centre, sanctions-split footer from `*_sanctions`/`_non_sanctions`, empty state when `qa_inspected_today`=0); comment mix from `comment_distribution`, role mix from `per_role_volume`, Sanctions/Standard split (substitute for TOP/MID/BOT stratum); reminder-backlog count (per-record list not emitted → footnote); "Quality flags this week" = real `qa_changed_today` count (substitute for 3-strike / 2-week review); recent-activity table (8 records from emitted cohort arrays, deduped — WHEN "—" since no per-record timestamps, IMO / Company / Scenario / Team chip / QA reviewer / Status pill); deep-dive panel = team agents ranked by `tagged_today`, click row to switch. Substitutions (no fabrication): 30-day history → Today-vs-floor + cohort avg, days-above-floor/streak/joined-date/Day-on-team → omitted, P50 tag time → Records/hour, TOP/MID/BOT stratum → Sanctions/Standard, 3-strike → Quality-flags count, 2-week review history → omitted, per-record reminder list → count only. (Hourly chart sourced `by_agent.hourly_buckets` here — fixed in PR #138.)
- **PR #138 — Agent Scorecard: hourly chart sources from `ownership_completions` (fixes BUG 1)** (GitHub #138, merge `70ea4ca`): the "Hourly tagging" panel read `by_agent[name].hourly_buckets`, which the aggregator buckets by the Airtable `start_tagging_date` field — a batch/task-shared timestamp (e.g. 672 records at exactly 09:24, 455 at 17:08), so every record in a task collapsed into the one hour the batch was stamped and the chart clumped at a single bar. Fix (dashboard-only, forward-only, no pipeline change): switched the panel's data source to `STATE.completions[name]` — the per-record `ownership_completions` table the Hourly Output heatmap already uses (same `canonicalAgent` keying, same `{hour: count}` shape, same `(getUTCHours()+3)%24` EAT-hour formula); lazy-loads completions if not yet fetched (Loading / failed states); footnote updated to note it's detector observation time (bursty, lags real tag time). Cross-check (Merline Akinyi): new bars distribute across 8 hours (6–13) and match the Hourly Output heatmap row exactly, vs the old source which clumped all 283 records into hour 9. `by_agent.hourly_buckets` left in the aggregator (out of scope, just no longer read here). BUG 2 (Hourly Output heatmap observation-time/burst behaviour — confirmed not a TZ off-by-one) remains separately diagnosed, untouched.
- **PR #140 — Hourly Output: relabel as observation-time (BUG 2)** (GitHub #140, merge `a0e3b59`): labelling-only follow-up to the BUG 2 diagnosis — the heatmap buckets by `completed_at` (the detector's first-observation clock, bulk-stamped per 5-min poll cycle), so finalization bursts shift the apparent peak later than real tag time; it's correct (EAT formula + continuous cron verified), not a TZ off-by-one, so this honestly labels it rather than changing data. Page subtitle now leads with "Observation time · bucketed when the detector first saw each record — may lag real tag time in bursts" (was the misleading "waiting → tagged transitions by EAT hour"); the heatmap header gains a visible amber "ⓘ observation time" pill (reuses `.pill-inline` warn colours) with the full burst-lag explanation on hover, and its meta sub-line is reworded to "Observation time · 06:00–23:00 EAT · rows colour-coded by team · bursts may shift the apparent peak later than real tag time"; footer note retained unchanged. Dashboard-only, forward-only, no pipeline change — `renderHourlyGrid` / `colSums` / cell-building code untouched (diff is only the two label lines); real-boot harness confirms PER-HOUR cells equal displayed agent-row sums (grid self-consistent, byte-identical to before), zero console errors.
- **PR #143 — Comparison detail layer with 30-day snapshot history** (GitHub #143, merge `adc9fa0`): rebuilds `PAGE_RENDERERS.comparison` as a Vivid detail layer driven by REAL snapshot history (`snapshots/<date>.json`), reusing the PR #131 side panel for the agent/team picker — dashboard-only, no pipeline change (snapshots already exist). New `loadHistory(daysBack)` loads the last N daily snapshots into `STATE.history` (cache `obj|null`, bounded concurrency ≤5, today from the live block); `_seriesForAgent`/`_seriesForTeam`/`_seriesForDates` + `_cmpStats` aggregate per-day rows (counts summed, rates recomputed from sums, missing days = honest gaps). Editorial head + Share/Export/Save (Export = real CSV diff; Share/Save visual); mode tabs (Agent / Team / Period — all real) + range tabs (7/30/90d); two comparison cards (A green / B cyan, real streak-above-floor) + VS + LEADER chip; 4 KPI delta cards (Daily output 30d avg, Days above floor, QA reject 30d, Sanctions tagged); two REAL dual-line Chart.js rollups (Productivity overlay vs floor-280 dashed, QA reject vs 2% dashed); full scorecard (Throughput / Quality / Reminders, diff = A−B); mirrored comment-scenario mix; per-side reminder cards; algorithmic verdict (per-KPI wins + A/ties/B tally). Picker side panel = agent union across history + 5 teams, click to set side A/B. Audit: only 12 snapshots exist (2026-05-17→28, no gaps), so a 30d request shows the 12 real days + an honest "N of 30 days have snapshots" pill with chart gaps. Substitutions (no fabrication): P50/P90 tag time + SPEED section dropped (per-record tag time isn't tracked — `start_tagging` is batch-level, per BUG 1) → 4th KPI = sanctions volume; active strikes + 90-day/2-week reviews dropped (not tracked); joined-date/day-on-team dropped; reminders open / aged>14d are real (`reminder_open`/`reminder_overdue`). Real-boot harness (`init()` → `goto('comparison')`) 16/16, zero console errors — both charts show real multi-day lines, picker switches sides + closes, all 3 modes + 7d range render.

## Phase A — 2026-05-17

The app now sees the full operation, not 9 of 17 fields.

### Data layer
- **Airtable schema verified** against the live `relations_support` table. Snapshot saved at `Ownership Operations Hub/airtable_schema_snapshot.json` (40 fields, all singleSelect choice IDs incl. the `Selected for BO QA ` trailing-space preservation).
- **`ww_audit_log.json.config.field_ids` expanded from 16 → 24 entries.** Added `comment`, `reminder`, `source_flow`, `add_new_company`, `requested_by`, `valid_done_by_bo`, `role`, `start_date`. `cross_table_field_ids` block documents `reason_for_the_change` (lives in `relations_io`, Phase C). `comment_choices` (11 canonical values) and `ww_qa_choices` added for downstream rendering.
- **Design doc was off-by-one on `comment`**: listed 12 values, Airtable has 11. The 11 are now the canonical set in `extract_v2.COMMENT_VALUES` and the config.
- **`POLL_PROCEDURE.md` patched**: dropped the `fieldIds` whitelist on the recent-window fetch — let Airtable return all fields. The aggregator picks what it needs via `extract_v2.FIELD_IDS`.

### Polling pipeline
- **`extract()` lifted into `.poll_work/extract_v2.py`** as a shared module — single source of truth for `FIELD_IDS`, the 11 canonical `COMMENT_VALUES`, the `SELECTED_FOR_BO_QA` constant (trailing space preserved), and `resolve_qa()` for QA-attribution fallback.
- **QA attribution switched from `last_modified_by` to `qa_assignee`**, with fallback only when blank. Each `qa_events` entry now records `qa_attribution_source` and the poller logs a `qa_attribution_fallback_count` so we can spot data-quality drift.
- Garbage values filtered at extract time: the self-referential `role == "role"` choice, the stale `status` values (`Cargill_Bulk_Carrier_2023-2`, `Leopard`).
- `Selected for BO QA ` (trailing space) replaced with the imported constant in all comparisons.

### Aggregator (`.poll_work/aggregate_v2.py`)
- New file. Computes per-(team, agent, qa, ww_qa) metrics described in `DESIGN_OWNERSHIP_INSIGHTS_V1.md §4.2` and writes to `daily_aggregates.json.aggregates_v2`. The legacy `by_agent` / `by_team` / `totals` block is preserved untouched alongside.
- Metrics: `counts_by_verification_status`, `counts_by_qa_status`, `comment_distribution` (11 buckets), `source_flow_distribution`, `per_role_volume`, `add_new_company_open`, `reminder_open`/`reminder_overdue`, `tagged_today`, `done_today`, `in_bo_qa_today`, `qa_inspected_today`, `qa_changed_today`, `need_to_be_update_today`, `ww_qa_throughput`, `ww_qa_change_rate`, `bo_qa_backlog`, `ww_qa_backlog`, `sampling_actual_pct` (3-component), `reject_rate`, full `lead_time_seconds` block (p50/p90 for 3 transitions).
- **Two intake metrics** (replace `daily_intake`):
  - `relations_support_intake_today` (totals only) — whole-table count of records created today regardless of assignee. **Today: 1,792.**
  - `team_routed_intake_today` (per-slice) — records created today and assigned to a team member. **Today: 0** — intake is being absorbed upstream, not by the 5 ownership teams.
- `reason_for_change_missing` deferred to Phase C (cross-table).
- **Sampling formula**: 3-component `(in_bo_qa + qa_inspected + need_to_be_update) / tagged_today`. Matches the existing aggregator. Target 25%.
- **Slimmed `by_qa` and `by_ww_qa`** dimensions to only the metrics that make sense at that grain.
- **Case-insensitive assignee matching** (mirrors the poller). Fixes a real bug: 72 records of "Hellen vigehi" were being dropped vs the roster's "Hellen Vigehi".
- **35 regression tests** in `.poll_work/tests/test_aggregate_v2.py` covering every metric, the slimming, the case-insensitive lookup, and `relations_support_intake_today`'s totals-only emission.

### Roster
- **`config/roster.json` is the new source of truth.** Shape: `teams.<TEAM>.{members, qa, ww_qa}`. `ww_qa: null` across all 5 teams pending operational sign-off.
- `ww_audit_log.json.roster` reduced to a deprecation shim mirroring the new file (renamed `agents` → `members`, added `ww_qa: null`).
- All readers updated: `run_poll_cycle7.py`, `aggregate_v2.py`, `render_dashboard.py`, `dashboard.html`. Fall back to the shim if `config/roster.json` is missing.

### Dashboard
- **`dashboard.html` rebuilt as a 7-page multi-nav UI.** Left-nav, top-bar freshness pill + refresh button, `localStorage`-persisted page + agent selection. Chart.js + Grid.js from CDN only.
- 5 pages fully implemented: **Overview**, **Agent Scorecard**, **BO QA Console** (live Airtable read-only fetch for today's tagged worklist + 5/5/5 stratification helper + add_new_company queue), **Daily Intake & Assignments**, **WW QA Console** (empty-state copy when no activity).
- 2 pages stubbed with a "Coming in Phase C/D" card + planned-widget list: **Pipeline & Lead Time**, **Case Scenarios**.
- **Team Activity Alerts** card on Overview fires when a team's `tagged_today` is 0 (the >50% 7-day drop rule will activate once we have 7 days of history).
- Old single-tab layout (Hourly Output, Pending QA, Productivity, Sampling, Silent Changes, Daily Verdicts) is gone; the same information lives across the 5 new pages.

### Documentation
- **`Ownership Operations Hub/airtable_schema_snapshot.json`** — frozen schema reference with field IDs + choice IDs + cross-table notes.
- **`Ownership Operations Hub/TECH_DEBT.md`** — 5 follow-ups for later sprints (20-page cap, `status`/`valid_done_by_bo` unused, WW QA dormancy, cross-table reason_for_change, add_new_company moderation queue).
- **`ARCHITECTURE.md`** collapsed from 7 competing drafts (`ARCHITECTURE.md` + `ARCHITECTURE_V2..V6.md`) into one ~50-line operating map pointing at `DESIGN_OWNERSHIP_INSIGHTS_V1.md` as the current source of truth.

### Housekeeping
- Deleted stale scripts: `run_poll.py`, `run_poll_v2.py`, `run_poll_now.py`, `run_poll_cycle.py`, `build_records.py` (hand-built snapshot constructor, not a live fetcher).
- Deleted orphan data files: `records.json`, `records_fetched.json`, `recent_001..003.json`, `boqa_001..002.json`.
- Cache refreshed (read-only Airtable fetch): 20 recent pages (2,000 records) + 1 BO QA page (96 records). Polling state was NOT advanced; this was a cache-only refresh, not a real poll cycle.

### Test results
- `.poll_work/tests/test_extract_v2.py`: 12/12 pass
- `.poll_work/tests/test_aggregate_v2.py`: 35/35 pass

### Known operational signals (not bugs)
- **Sampling 94% vs 25% target** — real over-sampling; conversation for ops.
- **Pweza dropped 566 → 8 records** in the 24h cache window.
- **Nyati 0 records** in the cache window — likely real workflow lull.
- **WW QA stage unused** — 0 of 264 in-scope records have `ww_qa_assignee` set; 6 records sit in `Selected for WW QA` with no assignee. See `TECH_DEBT.md` item 3.

### Patches
- **Phase A — patch: `aggregates_v2.computed_at` and per-(team, agent, totals) `hourly_buckets` (6→23 EAT, zero-filled)**; dashboard freshness pill prefers `computed_at`; Agent Scorecard heatmap reads from `hourly_buckets`.
- **Phase A — patch: canonicalize agent grouping key.** `_build_ownership_assignees` now returns `{lowercase_name → {team, canonical}}`; `aggregate()` stamps the canonical roster spelling onto each in-scope record before grouping, so `by_agent` keys match the roster exactly (e.g. `"Hellen Vigehi"`, `"Faith Khalai"`, `"Lillian Gichamba"`) instead of Airtable's raw casing/aliases.
- **Phase F1 — Deploy artifacts. New `deploy/` folder with auth-gated dashboard (Supabase magic-link, `@impactoutsourcing.co.ke` domain restriction). New `sync_to_supabase.py` for manual uploads. Local dashboard and polling unchanged.** `deploy/index.html` is a fork of the local dashboard: adds the Supabase JS SDK + a centered login overlay (email input, "Send magic link" button, inline domain-check error before any auth call); on session, hides the overlay and shows a `<email> · Sign out` pill in the top-right. Data loaders rewritten to read from Supabase Storage public URLs (`{SUPABASE_URL}/storage/v1/object/public/dashboard-data/<path>`) and the SDK's `storage.list()` for cache discovery — no more bash-cat or local HTTP fetches. Path constants flattened (`.poll_work/snapshots/` → `snapshots/`, `.poll_work/recent_p*` → `cache/recent_p*`, etc.). Adds a "Synced Nm ago" pill driven by `last_sync.json`. `deploy/vercel.json` rewrites everything to `/index.html` and forces `X-Frame-Options: DENY`. `sync_to_supabase.py` reads `.env.local` (`SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`), uses raw `requests` against the Storage REST API with `x-upsert: true`, walks `daily_aggregates.json` + `config/roster.json` + `ww_audit_log.json` + every `snapshots/*.json` + every cache `{recent,intake,boqa}_p*.json`, writes a per-folder summary and `last_sync.json`. Individual failures don't abort. New files: `.env.local.example`, `.gitignore` (adds `.env.local`, `.env.*.local`), `DEPLOY_GUIDE.md`. No changes to local `dashboard.html`, polling, or aggregator.
- **Phase E — Daily snapshot persistence. aggregate_v2 writes per-day snapshots to `.poll_work/snapshots/`. Weekly Report reads them and renders true multi-day rollups (Team Performance, Daily Activity, Total Completions, Agents Not Working).** Three new aggregator helpers: `_save_snapshot(aggs_v2, work_dir)` writes the `aggregates_v2` block to `.poll_work/snapshots/<YYYY-MM-DD>.json` (creates dir, overwrites same-day, pretty-printed); `load_snapshots(work_dir, start, end)` returns `[(date, aggs), ...]` for snapshots in range, silently skipping gaps; `compute_weekly_rollup(snapshots, roster=None)` produces `{date_range, days_with_data, totals (sums + active_agents_avg + qa_reviews/changes), per_day, per_team_rollup (records/unique_imos_sum/top_performer), agents_not_working (per-roster-member missed-days list)}`. `unique_imos_union` is approximated as sum-of-per-day-unique with a footnote (true set union needs record-level snapshots — Phase E2). `main()` calls `_save_snapshot` after writing `daily_aggregates.json`. Dashboard: Weekly Report rewritten — async snapshot loading via `loadWeeklySnapshots()` with `STATE.weeklySnapshots` cache (keyed by date, null = tried-and-missing); dynamic banner replaces the old amber today-only message ("Showing N days … Missing days: M" or "No snapshots yet — first will land at the next poll cycle"); real multi-day KPIs, real multi-day stacked Daily Activity by Team chart, real Total Completions chart (Flow A/C stacked + Flow B line), real Agents Not Working table (cross-day, capped at 5 missing-dates with ellipsis); per-day `<details>` sections with Flow A/C leaders (today defaults open, historical days closed). 9 new tests covering `_save_snapshot` round-trip, `load_snapshots` range filtering + gap handling + same-day overwrite, and `compute_weekly_rollup` (sums, gap skipping, active_agents_avg, per-team top performer, agents_not_working detection, empty-input safety).
- **Phase D — Windward parity: Flow A/B/C framework, Unique IMOs, QA Reviewer aggregations, aging tracker, per-task QA coverage. New pages: QA Performance, Weekly Report (today-only for now).** Aggregator: `compute_metrics` adds `flow_a_count` (Done/Valid w/o qa_assignee), `flow_b_count` (qa_assignee+qa_status both filled), `flow_c_count` (Done/Valid AND has QA), `total_completions` (A+C), and `unique_imos` per slice. New top-level emissions: `qa_reviewers` (per-QA reviews/approvals/changes/approval_pct/avg+median+p90 review_time_hours, sorted desc), `not_yet_finalized` (records in incomplete states with `days_open` + `open_roles_count` excluding self, capped 500 with `not_yet_finalized_truncated` flag), `qa_done_not_finalized` subset (QA'd but not closed — operational red flag). `compute_task_breakdowns` adds `completed`, `qa_reviewed`, `qa_coverage_pct` per task. Dashboard: 2 new pages — **QA Performance** (KPIs + sortable per-QA table with green<12h/amber<24h/red≥24h response coloring + reviews-per-QA bar chart) and **Weekly Report** (date-range picker UI + amber today-only note + KPI row + Team Performance table + Daily Activity / Total Completions charts + Agents Not Working table + per-day expandable section with Flow A/C leaders + 20-oldest Not Yet Finalized). Enhancements: Overview adds Unique IMOs KPI and a Flow A/B/C panel; Agent Scorecard adds Unique IMOs KPI and a Flow A/B/C stacked horizontal bar; Tasks page adds 3 columns (Completed, QA Reviewed, QA % with ≥15%/5-14%/<5% color thresholds).
- **Phase A — patch: three targeted fetches in POLL_PROCEDURE.md + metadata-based intake total.** Split the single past-24h fire-hose into Fetch A (ownership-team work, ~1k records vs the old ~12k), Fetch B (today's table intake, 30-page cap, sorted Created desc), and Fetch C (BO QA queue, unchanged). The headline `aggregates_v2.totals.relations_support_intake_today` now comes from page 1's `metadata.totalRecordCount` (authoritative, no silent truncation); the cache itself holds the first 3,000 most-recent today-records. New `aggregates_v2.totals.intake_partial: true` fires when `metadata.totalRecordCount > 3000`; the dashboard's Daily Intake page shows an amber banner in that case. Aggregator's `_load_records` now unions `recent_p*` + `intake_p*` + `boqa_p*` and dedups by record id. Dashboard browser-mode `listFiles()` probes both `recent_p*` and `intake_p*`. Tests for dedup + the metadata helper.
- **Phase A — patch: Tasks page (lifecycle tracking + flags) + multi-assignee extraction fix.** `extract_v2.extract()` now returns `assignees` (full list) alongside `assignee` (primary, first item — backward-compat preserved). New `compute_task_breakdowns()` in `aggregate_v2.py` groups every cached record by `requested_by` and emits a per-task dict with status distribution, completion counts, multi-assignee-aware agents-worked and teams-worked tables, and 5 operational flags (`incomplete`, `stuck`, `company-gap`, `unassigned`, `high-waiting`). The list lives at `aggregates_v2.tasks_all`, sorted by `date_last_modified` desc. New **Tasks** page in the dashboard (between Daily Intake and WW QA Console): KPI row (visible / incomplete / stuck / company-gap), search + view toggle (all/today/incomplete/stuck/flagged), 15-column sortable table, per-task detail drawer with status doughnut + completion breakdown + agents/teams tables + a read-only "Fetch all records" button that calls `list_records_for_table` filtered by `requested_by`. New doc: `Ownership Operations Hub/TASKS_TRACKING_NOTE.md`.
- **Phase A — patch: tighter completion definition + dead-vessel tracking + tasks-today breakdown.** Added `dead_vessel` field (fldK9xjvBASgXIKlm checkbox) to `extract_v2.FIELD_IDS` and `ww_audit_log.json.config.field_ids`. New `extract_v2.is_properly_completed()` helper — record counts as completed iff `start_date` is filled AND (`company_id` is filled OR `dead_vessel` is True); dead-vessel is the operational alternative when no company exists to link. Aggregator emits `properly_completed_today` + `dead_vessels_today` at totals/by_team/by_agent dimensions (skipped in QA/WW QA slim). New `tasks_today` + `tasks_today_count` at totals-only — list of `{name, records, is_sanctions}` grouped by `requested_by` for records created today, sorted desc, empty `requested_by` collapses to a `(no task name)` bucket. Dashboard: Overview adds two completion KPIs and a Dead vessels column to the per-team table; Agent Scorecard adds the same two KPIs (Properly completed shows the gap-to-tagged as a delta when positive); Daily Intake adds a Distinct-tasks KPI and a sortable Tasks panel (capped at 100 rows).
- **Phase A — patch: sanctions vs non-sanctions sampling split + Hourly Output page.** Operations runs two targets (≥15% non-sanctions, ≥50% sanctions) detected from `requested_by` via new `extract_v2.is_sanctions()`. `aggregate_v2.compute_metrics` splits the 5 today-counters into cohorts and emits `sampling_non_sanctions_pct` + `sampling_sanctions_pct` (with their own targets) and `reject_rate_sanctions` + `reject_rate_non_sanctions` alongside the existing combined metrics. Combined `sampling_actual_pct` (25%) kept for backward compat. Dashboard: Overview replaces single Sampling KPI with two side-by-side cohort KPIs and adds two columns to the per-team table; Agent Scorecard gains two cohort KPIs; BO QA Console tagged-today headline shows the split. New page **Hourly Output** (between Agent Scorecard and BO QA Console) renders an agents×hours heatmap grid grouped by team, with a team filter (persisted in localStorage as `wqa.hourlyTeam`) and an "All agents" totals row. New doc: `Ownership Operations Hub/SAMPLING_NOTE.md`.

### Out of scope (Phase A)
Phase C (WW QA full implementation, new-company moderation queue, cross-table `reason_for_change`), Phase D (lead-time funnel chart), Phase E (Next.js + Supabase replatform).
